name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - name: Checkout uploadkit-odoo
        uses: actions/checkout@v4
        with:
          path: uploadkit-odoo

      - name: Checkout uploadkit
        uses: actions/checkout@v4
        with:
          repository: uploadkit/uploadkit
          path: uploadkit

      - name: Checkout uploadkit-testing
        uses: actions/checkout@v4
        with:
          repository: uploadkit/uploadkit-testing
          path: uploadkit-testing

      - name: Checkout uploadkit-security
        uses: actions/checkout@v4
        with:
          repository: uploadkit/uploadkit-security
          path: uploadkit-security

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install packages
        working-directory: uploadkit-odoo
        run: |
          pip install -e ../uploadkit -e ../uploadkit-testing -e ../uploadkit-security
          pip install -e ".[dev]"

      - name: Test
        working-directory: uploadkit-odoo
        run: pytest -q

      - name: Coverage
        working-directory: uploadkit-odoo
        run: |
          coverage run -m pytest -q
          coverage report -m --include='src/*' --fail-under=100
